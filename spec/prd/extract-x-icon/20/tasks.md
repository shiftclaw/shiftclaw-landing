# Tasks — Issue #20

- [ ] Create `src/components/icons/x-icon.tsx` with Lucide-compatible API (size, className, SVG props)
- [ ] Replace inline X/Twitter SVG in `page.tsx` footer with `<XIcon />` import
- [ ] Verify `npm run build` passes
